/*
* Copyright 2026 NXP
* NXP Proprietary. This software is owned or controlled by NXP and may only be used strictly in
* accordance with the applicable license terms. By expressly accepting such terms or by downloading, installing,
* activating and/or otherwise using the software, you are agreeing that you have read, and that you agree to
* comply with and are bound by, such license terms.  If you do not agree to be bound by the applicable license
* terms, then you may not retain, install, activate or otherwise use the software.
*/

/**
 * ------------------------------------------------------------------------------
 * NOTICE: This is an auto-generated file
 * ------------------------------------------------------------------------------
 * This file was automatically generated by GUI Guider (NXP Semiconductors).
 * Please be aware that any manual modifications may be overwritten when the
 * code is regenerated.
 *
 * Tool            : GUI Guider
 * Version         : 2.0.0
 * Generated on    : 2026/8/18
 * ------------------------------------------------------------------------------
 */

#include "gg_event.h"
#include "custom.h"

static void card_pickup_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->Pickup.screen,
                .setup_fn  = setup_Pickup,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 180,
                .delay     = 0,
                .auto_del  = false,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void card_identify_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->Scan.screen,
                .setup_fn  = setup_Scan,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 180,
                .delay     = 0,
                .auto_del  = false,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void card_management_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->Login.screen,
                .setup_fn  = setup_Login,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 180,
                .delay     = 0,
                .auto_del  = false,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

void gg_event_init_Home(gg_ui_t * ui)
{
    lv_obj_add_event_cb(ui->Home.card_pickup, card_pickup_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->Home.card_identify, card_identify_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->Home.card_management, card_management_event_handler, LV_EVENT_ALL, ui);
}
