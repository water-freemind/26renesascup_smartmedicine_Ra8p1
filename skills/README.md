# 项目技能库

此目录随仓库同步，供 Codex、OpenCode 及其他协作 Agent 复用。每个技能必须是独立目录，并以 `SKILL.md` 作为入口。

- `rasc-configure-ra/`：安全修改、生成、审查 Renesas RA/FSP 的 `configuration.xml`。

不要直接修改 `ra_gen/`、`ra_cfg/` 等 RASC 生成目录；配置以 `configuration.xml` 为唯一来源。

